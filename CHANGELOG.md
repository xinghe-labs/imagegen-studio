# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的格式，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.6.1] - 2026-09-26

### 修复

- CI 上三平台暴露的测试竞态：激活/口令登录后用 `networkidle` 等待会在页面重载前就满足，
  断言跑早了；改为等待 `#profile-select` 出现（可跨 reload，无竞态）

## [1.6.0] - 2026-09-26

### 新增

- **独立的登录 / 激活页**：未认证时工作台让位给一张居中的进入卡片——「兑换码激活」
  （输入码 + 用户名即进，用户全程不接触 token 概念）和「访问口令」两个标签；
  `?invite=码` 链接打开自动预填并从地址栏剥离；错误码在卡片内提示。
  所有 401 路径统一收敛到该页（替代原先工作台上的死路提示条与报错 toast）

### 变更

- 管理员引导保持一次性服务器操作（`add_user.py add ... --admin` 打印链接点开即进）——
  这是安全底线：没有服务器侧引导凭证的话，第一个访问者就能自封管理员

## [1.5.0] - 2026-09-26

### 新增

- **邀请码分发**（替代点对点私发永久 token 链接）：管理员一键生成短期限次邀请码
  （默认 10 次 · 7 天），把码或 `/?invite=码` 链接发到群里，新人打开自己填用户名激活，
  一人一 token 一图库自动建好并直接登录；过期/用完自动失效，管理员可随时撤销；
  错误邀请码计入认证限流。管理区与 CLI（`add_user.py invite`）均可生成，users 文件热加载互通

## [1.4.3] - 2026-09-26

### 修复

- 更新面板在 GitHub 检查失败时静默无反馈（看起来和「已是最新」一模一样）：现在会显示
  「GitHub 检查失败（服务器连不上 GitHub？）」，悬停可看具体原因——此前服务器断外网时
  用户无法区分「没有新版本」和「查不了」

## [1.4.2] - 2026-09-26

### 新增

- **单令牌模式的「复制邀请链接」**：访问令牌行新增按钮，把 `{域名}/?token=当前令牌`
  拼好复制到剪贴板，发给对方即可登录（此前该按钮只在多用户管理模式下出现）

## [1.4.1] - 2026-09-26

### 新增

- **服务端一键自助更新**：更新面板对比 GitHub 最新 tag 与本服务端版本，落后时显示
  「GitHub 最新 vX · 部署文档」提示；管理员（users 模式管理员或单令牌持有者）可点
  「⬇ 一键更新服务端」——服务端自检后 `git pull --ff-only` + 安装依赖 + 原地重启，
  页面轮询到新版本后弹「刷新」提示。护栏：工作区有未提交改动拒更、pip 失败中止（继续运行旧版）、
  Docker 部署与非 git 仓库提示走各自方案、未启用认证的实例禁用该功能
- `GET /api/version-check`：服务端代理查询上游 tag（离线容错，结果缓存 1 小时，↻ 强制刷新）

## [1.4.0] - 2026-09-26

### 新增

- **管理员页面管理用户与令牌**：users 文件里带 `"admin": true` 的用户（或 `add_user.py add ... --admin`）
  登录后，「网关配置」面板出现「用户与令牌管理」区——添加用户并复制邀请链接、查看完整令牌、
  换发令牌（旧的立即失效）、移除用户（不动图库文件）；users 文件按 mtime 热加载，改动即时生效无需重启，
  与 `add_user.py` 命令行改动互通
- `/api/meta` 新增 `auth_mode`（users / token / open）与 `is_admin`；管理 API 仅限 users 模式下的管理员令牌
  （`GET/POST /api/users`、`POST /api/users/{name}/rotate`、`DELETE /api/users/{name}`，不能移除自己）

## [1.3.1] - 2026-09-26

### 变更

- 更新面板改为贴合本项目的紧凑样式：单行状态（✓ 已是最新 / ⬇ 服务端有新版 / 版本回退提示）替代大色块，
  按钮文案如实为「刷新加载新版」——页面只负责刷新加载前端，服务端本体升级走 DEPLOY.md 的自动更新方案
- 修复：面板状态行在某些状态下未显示（hidden 类未被移除），并补上可见性断言

## [1.3.0] - 2026-09-26

### 新增

- **版本更新面板**：头部版本徽章可点击，下拉显示当前版本、服务端版本与更新状态——
  有新版时徽章亮橙点、面板出现「有新版本可用！」提示框与「⬇ 立即更新」按钮（点击刷新加载新版前端）、
  「↻」手动立即检查、「查看更新日志」跳转 CHANGELOG；点面板外部或 Esc 关闭

## [1.2.0] - 2026-09-26

### 新增

- **网页自动更新**：页面每 5 分钟轮询一次 `/api/meta` 版本号（切回标签页时也立即查一次），服务端升级后开着的页面弹「服务端已更新 → 刷新」提示，点击即换到新版；前端静态文件本就带 `no-cache` + ETag 条件请求，正常刷新即得新版，无需强刷
- DEPLOY.md 新增「自动更新」章节：裸机 systemd timer 定时 git 更新、Docker 定时重建、GitHub Actions 推 tag 即部署三套方案

## [1.1.1] - 2026-09-26

全功能 E2E 探索（真实浏览器驱动）发现的四个交互层缺陷修复，全部附回归测试。

### 修复

- **预设 chip 无法选中** — `#preset` 的点击监听只保存草稿、从不切换选中态（自 chip UI 引入起即坏）：
  现在 fast / standard / 透明底可正常点击选中并传给引擎
- **删除后的「撤销」按钮无法点击** — toast 容器 `pointer-events: none` 未在 toast 上恢复（撤销按钮看得见、点不着）：
  现在 `.toast` 自身恢复 `pointer-events: auto`，撤销真正可用
- **令牌模式首访是死路** — `init()` 先 `await loadMeta()`，无令牌时 401 中断，令牌面板的保存/清除按钮事件从未绑定，
  认证提示引导的路径走不通：现改为先绑定全部事件再拉数据，认证提示引导的流程可走通
- **backup_library.py 同秒重跑崩溃** — 快照目录名（秒级时间戳）冲突时 `copytree` 抛 FileExistsError：现在自动追加序号
- 顺手修正：`init().catch` 里 toast 的 `"error"` 级别参数写在了函数调用外，错误提示一直以 info 样式展示

### 测试

- UI 冒烟新增 3 条回归（预设选中并传递、撤销恢复、令牌首访保存/清除）与 `TokenModeUITest`
- 新增 `tests/test_backup_library.py`（快照完整性、.trash 排除、同秒重跑、保留策略）

## [1.1.0] - 2026-09-26

自 v1.0.0 以来的 39 个提交，围绕「多人可用、图库可整理、故障可自愈」三个方向。

### 新增

- **多用户隔离** — users 文件一人一 token 一图库，历史、统计、生成产物、参考图互不可见，跨库读写一律 403；users 模式优先于单 token（d270b38）
- **图库整理** — 删除移入回收站（可撤销、7 天自动清理）、多选批量（全选 / Shift 区间 / 取消）打包下载与批量删除、按日期筛选；灯箱放大支持 ←/→ 翻页（84d142f、9c7991c）
- **草稿记忆** — 提示词与模型、张数、预设、比例、尺寸、格式、项目、模式全部自动记住，刷新页面原样恢复（84bfff3）
- **生成中取消** — 排队中直接标记取消，运行中终止子进程并清理半成品（84bfff3）
- **桌面运维** — 每日备份计划任务注册与登录自启安装脚本（Windows），库快照支持保留份数（c6716ad、d504d23）
- **认证防爆破** — 连续 10 次认证失败锁定来源 5 分钟（84d142f）
- **凭据引导** — 无网关凭据时生成按钮快速失败并显示提示横幅（7dcb65d、11e9d2f）
- **访问令牌面板** — 部署到带令牌的服务器时可在页面上保存/清除 token（84d142f）
- **版本号展示** — 页面头部与 `/api/meta` 返回应用版本（本次引入）

### 修复

- 网关瞬时故障（空响应、断连、超时等）自动重试：CLI 用满重试预算，服务端把整个任务再跑一次（232bdc2、6031ad9）
- 生成进程启动失败不再悬挂任务（dafd229）
- 提示词长度上限、sidecar 原子写入、profile base_url 自动补 `/v1`（dafd229、f76be51）
- 图生图遵循高级面板的尺寸与格式设置（b192745）
- 显式指定模型时不再重复传 `--choice`（ebb27b1、391820a）
- 异步剪贴板 API 被拒绝时回退 `execCommand`（915aa28）

### 变更

- 生成器面板按语义重新分组；预设与自定义尺寸面板去重（cc1ffbb、781e3a1）
- 视觉：暗房双栏工作台主题、天蓝配色、瀑布流图库、卡片快捷操作、可视化比例选择、toast 提示（fdfb659…7ff841e 多轮打磨）
- profile 下拉只显示名称，URL 移入 tooltip（5b4f65b）
- 移除「复现命令」「提示词库」「角色一致性」功能，保持生成器聚焦（cc0a6e1、3ba92dd、91d0ccd）

## [1.0.0] - 2026-09-19

首个正式版：生成器优先的中文生图工作台，跑在 image-gen 引擎上。

- 文生图 / 图生图，任务进度实时显示
- 多网关 profile 管理（API key 只存服务端配置文件，页面永不回传）
- sidecar 账本驱动的图库：按模型/项目/关键词筛选、收藏、评分
- 项目分组与用量统计
- 单令牌认证；Docker / systemd 部署文档与本地启动脚本
